---
description: Feedback Gate Protocol - AI Agent 反馈关卡（可选）
globs:
alwaysApply: false
---

# Feedback Gate Protocol（可选）

本规则**默认不附加到每条对话**。仅在用户明确要求使用 Feedback Gate、或需要弹窗收集反馈时，再调用 `feedback_gate_chat` MCP 工具。

## 何时使用

- 用户主动要求「用 Feedback Gate 收反馈」「弹窗确认」等。
- 长任务需要显式「完成」信号时再打开弹窗，避免每轮回复都触发 MCP。

## 工具

- `feedback_gate_chat`：按需调用；**不要**在每次普通回复末尾强制调用。
- 若工具不可用，告知用户 MCP 服务器可能未运行。

## 运行长进程

运行整个应用（Web 服务器、长期进程等）前需先征得用户确认。
