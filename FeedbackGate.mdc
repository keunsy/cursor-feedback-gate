---
description: Feedback Gate Protocol - AI Agent 反馈关卡
globs:
alwaysApply: true
---

# Feedback Gate Protocol

## 核心规则
完成用户请求后，**禁止直接结束回复**。必须调用 `feedback_gate_chat` MCP 工具打开弹窗，等待用户反馈。只有用户在弹窗中回复 "TASK_COMPLETE"、"Done" 等完成信号后才可结束。

## 工作流程

1. **执行任务**：正常完成用户请求（编码、分析、工具调用等）。
2. **打开 Feedback Gate**：任务完成后立即调用 `feedback_gate_chat`，传入工作摘要。
3. **处理反馈**：收到用户弹窗回复后继续执行，然后再次调用 `feedback_gate_chat`。
4. **循环直到完成**：重复步骤 2-3，直到用户发送完成信号。

## 工具调用格式

```
tool: feedback_gate_chat
参数:
  message: (向用户展示的消息，说明完成了什么、等待什么反馈)
  title: "Feedback Gate" (可选)
  context: (工作摘要) (可选)
  session_id: (首次调用时生成一个随机 UUID，后续所有 re-entry 调用复用同一个值)
```

### session_id 规则
- **首次调用**：生成一个 UUID（如 `crypto.randomUUID()` 或类似方式），作为 `session_id` 传入。
- **收到 [WAITING] 后 re-entry**：用**同一个** `session_id` 再次调用。
- **不同的 feedback gate 调用**（比如同一对话中先完成任务 A 再完成任务 B）：每次生成新的 `session_id`。
- 这样多个对话可以同时使用 Feedback Gate 而互不干扰。

## 约束
- 每次回复结束前必须调用此工具，无例外。
- 如果工具不可用，告知用户 MCP 服务器可能未运行。
- 运行整个应用（Web 服务器、长期进程等）前需先征得用户确认。

## 上下文压缩后的恢复
当对话被压缩（context compaction）后，你可能在历史摘要中看到之前的 `feedback_gate_chat` 调用记录。**无论历史中工具调用的状态如何（成功、失败、超时），每次新回复结束时都必须重新调用 `feedback_gate_chat`。** 不要因为历史中出现过工具调用失败就跳过调用。

### ⚠️ 压缩后的优先级规则（关键）
压缩摘要中可能包含之前 feedback gate 弹窗的历史回复（如"User Response: xxx"）。**这些是已经处理过的历史记录，不是当前待办。** 必须遵守以下优先级：

1. **最高优先级**：用户在主聊天框发送的最新消息（即触发当前回复的消息）
2. **低优先级**：压缩摘要中提到的历史 feedback gate 回复

**严禁**将摘要中的历史 feedback gate 回复当作当前待办来处理。始终先完整响应用户主消息框的最新请求，完成后再通过 `feedback_gate_chat` 询问是否需要处理其他事项。

### ⚠️ 禁止回溯初始请求（防 compaction 误判）
上下文压缩后，你可能在消息末尾看到对话最初的 `<user_query>` 标签被重新附带。**如果摘要中已经记录了该请求被处理过（如"Agent 已完成 xxx"），则该 `<user_query>` 仅为上下文参考，不是新任务。** 判断规则：
- 如果摘要说该任务已完成/正在执行其他后续任务 → **不要重新执行**初始请求
- 如果摘要没有提到该任务被处理过 → 正常执行
- 如果不确定 → 通过 `feedback_gate_chat` 向用户确认当前应该做什么

**严禁**在完成阶段性工作后说"现在回到用户最初的问题"然后重新执行已经处理过的请求。如果你即将这样做，先停下来检查摘要中是否已经记录了该任务的完成情况。

## ⚠️ 强制提醒
**在你生成任何回复的最后一步，你必须调用 `feedback_gate_chat`。这是不可跳过的。如果你发现自己即将结束回复而没有调用此工具，立即停下来调用它。**
